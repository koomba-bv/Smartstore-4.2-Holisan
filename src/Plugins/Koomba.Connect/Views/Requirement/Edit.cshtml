@model ChainRuleModel
@{
    Layout = "_AdminLayout";
    ViewBag.Title = T("Admin.Promotions.Discounts.EditDiscountDetails").Text;
}
@using (Html.BeginForm())
{
    <div class="section-header">
        <div class="title">
            @T("Plugins.Koomba.Connect.Requirement.EditTitle")
            <a href="@Url.Action("Edit", "Configuration", new { id = Model.ConnectionId })">(@T("Plugins.Koomba.Connect.Connection.BackToList"))</a>
        </div>
        <div class="options">
            @{ Html.RenderWidget("admin_button_toolbar_before"); }
            <button type="submit" name="save" value="save" class="btn btn-warning">
                <i class="fa fa-check"></i>
                <span>@T("Admin.Common.Save")</span>
            </button>
            <button type="submit" name="save" value="save-continue" class="btn btn-secondary">
                @T("Admin.Common.SaveContinue")
            </button>
            <button type="submit" name="discount-delete" id="discount-delete" class="btn btn-danger">
                <i class="far fa-trash-alt"></i>
                <span>@T("Admin.Common.Delete")</span>
            </button>
            @{ Html.RenderWidget("admin_button_toolbar_after"); }
        </div>
    </div>

    Html.RenderPartial("_CreateOrUpdate", Model);
}
@Html.DeleteConfirmation("discount-delete")