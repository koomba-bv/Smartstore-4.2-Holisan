@model ConnectionModel

@{
    Layout = "_AdminLayout";
    ViewBag.Title = T("Plugins.Koomba.Connect.Connection.Create").Text;
}

@using (Html.BeginForm())
{
    <div class="section-header">
        <div class="title">
            @T("Plugins.Koomba.Connect.Connection.Create")
            <a href="@Url.Action("ConfigurePlugin", "Plugin", new { area = "Admin", systemName = "Koomba.Connect" })">(@T("Plugins.Koomba.Connect.Connection.BackToList"))</a>
        </div>
        <div class="options">
            <button type="submit" name="save" value="save" class="btn btn-primary">
                <i class="fa fa-check"></i>
                <span>@T("Admin.Common.Save")</span>
            </button>
            <button type="submit" name="save" value="save-continue" class="btn btn-secondary">
                <span>@T("Admin.Common.SaveContinue")</span>
            </button>
        </div>
    </div>

    Html.RenderPartial("_CreateOrUpdate", Model);
}